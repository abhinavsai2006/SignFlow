# Frontend environment variables for PRODUCTION builds
# These are embedded at build time by Vite — they become static in the compiled bundle.
# Set VITE_API_URL to your Railway backend deployment URL.

VITE_API_URL=https://api.signflow.abhinavsai.com/api
